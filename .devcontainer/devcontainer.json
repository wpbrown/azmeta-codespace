{
  "extensions": [
    "ms-python.python"
  ],

  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash",
    "terminal.integrated.shellArgs.linux": ["-l"]
  },
  
  "dockerFile": "Dockerfile",

  "postCreateCommand": "./.devcontainer/post-create.sh 1> >(tee /tmp/post-create.out ) 2> >(tee /tmp/post-create.err >&2 )"
}
